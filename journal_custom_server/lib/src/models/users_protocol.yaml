class: Users
table: users
fields:
  login: String
  passwordHash: String
  roleId: int, relation(parent=roles)
  personId: int, relation(parent=person)
indexes:
  users_person_unique_idx:
    fields: personId
    unique: true

#   teacherId: int?, relation(parent=teachers)
#   studentId: int?, relation(parent=students)
# indexes:
#   unique_teacher:
#     fields: teacherId
#     unique: true
#   unique_student:
#     fields: studentId
#     unique: true