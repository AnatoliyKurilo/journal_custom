class: Attendance
table: attendance
fields:
  # classId: int, relation(parent=classes)
  # studentId: int, relation(parent=students)
  classes: Classes?, relation
  students: Students?, relation
  # status: String
  isPresent: bool # true - присутствовал, false - отсутствовал

  comment: String?
